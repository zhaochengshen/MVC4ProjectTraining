@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <div>
        @using (Html.BeginForm("RadioButton", "Test2"))
        {
            @: 可供选择的颜色:<br />
            @: 红 @Html.RadioButton("Color", "红色", true)
            @: 蓝 @Html.RadioButton("Color", "蓝色")
            @: 绿 @Html.RadioButton("Color", "绿色") 
            <input type="submit" value="提交" />
        }
    </div>
</body>
</html>
